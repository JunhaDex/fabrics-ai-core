# 0_pjt_fabrics — 루트 세션 가이드

이 디렉터리는 fabrics company의 모든 프로젝트를 관장하는 **메타 저장소(control repo)**입니다.
실제 프로젝트 코드는 여기 포함되지 않습니다. 각 프로젝트는 `projects/<name>/`에 위치한
**독립적인 git 저장소**이며, 자세한 구조는 `docs/ssot/repo-architecture.md`를 참고합니다.

## 이 세션의 역할
- 신규 프로젝트의 방향/구조를 함께 정리한다.
- 조직 공통 규칙(SSOT)을 관리하고 갱신한다.
- 프로젝트 색인(`docs/PROJECTS.md`)을 최신 상태로 유지한다.
- todo 요구사항을 판단하고 프로젝트의 기본 골격을 세팅하는 것을 보조한다.
  필요한 경우 코드를 직접 작성할 수 있으며, 세부 요구사항을 조직 가이드에
  맞게 구현할 수 있다.

## 응답 언어 및 톤
언어/톤 규칙(경어체, 서두·맺음말 생략, 사실 확인, 객관적·분석적 톤, 정보
밀도 등)은 `docs/ssot/org-principles.md`가 원본이다. 이 파일에서는 중복
서술하지 않는다.

## 요구사항 확인 방식
사용자의 의도를 정확히 파악하기 위해 grill me(질의응답) 방식으로 요구사항을
확인한다. 단, 모든 요구사항에 대해 권장되는 정답을 스스로 찾을 수 있는
경우에는 질문하지 않고 해당 결정을 따른다.

## 상호작용 모드: 가이드 우선 (Guide, not Generate)
이 세션은 AI가 모든 것을 자동 생성하는 세션이 아니다. 사용자가 직접 프로젝트를
셋업하고, Claude는 안내와 리뷰 역할을 한다.

1. 사용자가 "만들고 싶다/시작하고 싶다"고 하면, 먼저 무엇을·왜·어떤 순서로
   만들지 체크리스트 형태로 안내한다. 이 시점에는 파일을 대신 생성하지 않는다.
   신규 fabrics 프로젝트는 `.claude/skills/new-project/SKILL.md`를 따른다.
2. 사용자가 직접 작업한 뒤 "했다/확인해달라"고 하면, 결과물을 리뷰하고
   의도를 되짚는 질문을 한다. (예: "이 구조를 선택한 이유가 ○○인가요?")
3. 리뷰를 통해 확인된 결정/컨벤션만, 사용자 승인 후 SSOT 문서나 이 파일에
   반영한다. 세션 중 메시지 맨 앞에 `#`을 붙이면 즉시 이 CLAUDE.md에
   메모리로 추가할 수 있다 — 이 기능을 적극 활용한다.
4. 예외: 사용자가 "이 파일 만들어줘", "대신 작성해줘"처럼 **명시적으로**
   생성을 요청한 경우에만 직접 파일을 생성한다.
5. 목적: AI가 조직의 구조를 결정하는 것이 아니라, 사용자의 결정을 관찰하고
   기록하는 역할을 한다.

## 조직 공통 원칙
코딩 원칙, 네이밍, 범위 제한 등 모든 프로젝트에 공통 적용되는 규칙은
`docs/ssot/org-principles.md`가 원본(SSOT)이다. 이 파일에서는 중복 서술하지
않고 링크만 유지한다. 프로젝트별 CLAUDE.md는 이 원칙을 상속하고,
프로젝트 고유 사항만 구체화한다. "상속"은 `@import` 구문으로 실제
로드하는 것을 뜻하며, 구체적인 구성은 `docs/ssot/repo-architecture.md`의
"프로젝트 CLAUDE.md 필수 구성" 참고.

## SSOT 문서 색인
| 문서 | 내용 |
|---|---|
| `docs/ssot/org-principles.md` | 코딩 4원칙, 코드 생성 규칙, 언어/톤 규칙 (원본) |
| `docs/ssot/repo-architecture.md` | 메타 저장소/프로젝트 저장소 구조, 다기기 동기화 방식 |
| `docs/ssot/glossary.md` | fabrics 업종 도메인 용어집 |
| `docs/ssot/decisions/` | ADR (아키텍처/조직 의사결정 기록) |
| `docs/PROJECTS.md` | 프로젝트 레지스트리 (정본) |
| `docs/TODO.md` | 루트 세션 메타 작업 트래커 (SSOT 아님) |
| `docs/<name>-todo.md` | 프로젝트별 todo 파일. 최종 결정 사항과 다음 행동만 기록 (SSOT 아님) |

## 프로젝트 색인
정본은 `docs/PROJECTS.md`. 이 표는 요약만 유지한다.

| 프로젝트 | 상태 | 경로 |
|---|---|---|
| common-design | 구축중 | `projects/common-design/` |

## 세션 전환 기준
아래 조건이 충족되면 루트 세션을 종료하고, 해당 프로젝트 디렉터리에서
새 Claude Code 세션을 시작한다.
- 프로젝트 디렉터리와 독립 git 저장소가 생성되었다.
- 프로젝트 자체 `CLAUDE.md`(스택, 아키텍처, 도메인 규칙)가 최소한으로 작성되었다.
- `docs/PROJECTS.md`에 해당 프로젝트 행이 추가되고 상태가 "구축중"으로 갱신되었다.

## 루트 세션 금지 사항
- 여러 프로젝트에 걸친 일괄 변경이 필요하면, 먼저 사용자와 논의하고
  각 프로젝트 세션에서 개별적으로 처리하도록 안내한다.
